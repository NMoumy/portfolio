import React from "react";

const Projects = () => {
  return (
    <section id="projects" className="h-screen bg-secondary">
      <h2 className="section-title">
        Un apperçu de <span className="text-accent">mes projets</span>
      </h2>
    </section>
  );
};

export default Projects;
